# Program that reads elements from the input tape until
# it finds a 0 and writes double the value read to the
# output tape (the value 0 is not copied)

bucle:	READ 1
				LOAD 1
				JZERO fin
				LOAD 1
				MUL =2
				STORE 1
				WRITE 1
				JUMP bucle
fin:		HALT
