# Program that reads the elements from the input tape until
# it finds a 0 and copies the read values to the output tape
# (the value 0 is not copied)

lee:	read [1]
			load 1[2
			jzero fin
			write [1
			jump lee
fin:	halt
